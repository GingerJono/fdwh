trigger:
- master

variables:
  buildConfiguration: 'Release'
  publishDir: '$(Build.ArtifactStagingDirectory)/published'

stages:
# ---------------------------
# BUILD + PUBLISH
# ---------------------------
- stage: BuildAndPublish
  displayName: 'Build and Publish Blazor App'
  jobs:
  - job: Build
    displayName: Build
    steps:
    - task: UseDotNet@2
      displayName: 'Install .NET 8 SDK'
      inputs:
        packageType: 'sdk'
        version: '8.0.408'

    - task: DotNetCoreCLI@2
      displayName: 'Restore NuGet Packages'
      inputs:
        command: 'restore'
        projects: 'Sandbox.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build Solution'
      inputs:
        command: 'build'
        arguments: '--configuration $(buildConfiguration)'
        projects: 'Sandbox.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Publish Project'
      inputs:
        command: 'publish'
        projects: 'Sandbox.csproj'
        arguments: '--configuration $(buildConfiguration) --output $(publishDir)'
        publishWebProjects: false
        zipAfterPublish: false

    - task: PowerShell@2
      displayName: 'Extract Associated Work Item ID'
      env:
        SYSTEM_ACCESSTOKEN: $(System.AccessToken)
      inputs:
        targetType: 'inline'
        script: |
            [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
            $org = "DaleBI"
            $project = "DaleSandboxApp"
            $buildId = $env:BUILD_BUILDID
            $uri = "https://dev.azure.com/$org/$project/_apis/build/builds/$buildId/workitems?api-version=7.0"
            $headers = @{ Authorization = "Bearer $env:SYSTEM_ACCESSTOKEN" }
    
            try {
                $response = Invoke-RestMethod -Uri $uri -Headers $headers -Method Get
                if ($response.count -eq 0) {
                    Write-Warning "⚠️ No work items associated with this build."
                    exit 0
                }
    
                $workItemUrl = $response.value[0].url
                $workItemId = $workItemUrl -replace '^.+/workItems/', ''
                $output = @{ workItemId = $workItemId }
    
                $buildInfoDir = "$(Build.ArtifactStagingDirectory)/published/wwwroot"
                if (-not (Test-Path $buildInfoDir)) {
                    New-Item -Path $buildInfoDir -ItemType Directory -Force | Out-Null
                }
    
                $output | ConvertTo-Json | Set-Content -Path "$buildInfoDir/buildinfo.json"
                Write-Host "✅ Work item $workItemId saved to buildinfo.json"
            } catch {
                Write-Error "❌ Failed to fetch associated work item: $_"
                exit 1
            }

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Artifact'
      inputs:
        PathtoPublish: '$(publishDir)'
        ArtifactName: 'drop'

# ---------------------------
# DEPLOY DEV (approval via env)
# ---------------------------
- stage: Deploy_DEV
  displayName: 'Deploy to DEV'
  dependsOn: BuildAndPublish
  jobs:
  - template: Deployment/Templates/deploy-iis.yml
    parameters:
      environmentName: Development
      agentName: DUWPMDEV01
      targetPath: 'C:\inetpub\wwwroot\Sandbox2'
      iisAppPool: 'Sandbox2AppPool'

# ---------------------------
# DEPLOY PROD (approval via env)
# ---------------------------
- stage: Deploy_PROD
  displayName: 'Deploy to PROD'
  dependsOn: Deploy_DEV
  jobs:
  - template: Deployment/Templates/deploy-iis.yml
    parameters:
      environmentName: Production
      agentName: DUWPMLSW03
      targetPath: 'C:\inetpub\wwwroot\Sandbox2'
      iisAppPool: 'Sandbox2AppPool'
