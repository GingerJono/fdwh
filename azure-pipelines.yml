trigger:
- master  

pool:
  name: duwpmdev01 

variables:
  buildConfiguration: 'Release'
  publishDir: '$(Build.ArtifactStagingDirectory)/published'

stages:
- stage: BuildAndPublish
  displayName: 'Build and Publish Blazor App'
  jobs:
  - job: Build
    steps:
    - task: UseDotNet@2
      displayName: 'Install .NET 8 SDK'
      inputs:
        packageType: 'sdk'
        version: '8.0.408'

    - task: DotNetCoreCLI@2
      displayName: 'Restore NuGet Packages'
      inputs:
        command: 'restore'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Build Solution'
      inputs:
        command: 'build'
        arguments: '--configuration $(buildConfiguration)'
        projects: '**/*.csproj'

    - task: DotNetCoreCLI@2
      displayName: 'Publish Project'
      inputs:
        command: 'publish'
        projects: '**/*.csproj'
        arguments: '--configuration $(buildConfiguration) --output $(publishDir)'
        publishWebProjects: false
        zipAfterPublish: false

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Artifact'
      inputs:
        PathtoPublish: '$(publishDir)'
        ArtifactName: 'drop'

- stage: DeployToIIS
  displayName: 'Deploy to IIS Server'
  dependsOn: BuildAndPublish
  jobs:
  - job: Deploy
    steps:
    - download: current
      artifact: drop

    - task: PowerShell@2
      displayName: 'Stop IIS App Pool'
      inputs:
        targetType: 'inline'
        script: |
          Import-Module WebAdministration
          Stop-WebAppPool -Name "Sandbox2AppPool"

    - task: PowerShell@2
      displayName: 'Copy Files to IIS Folder with Exit Code Handling'
      inputs:
        targetType: 'inline'
        script: |
          $source = "$(Pipeline.Workspace)\drop"
          $destination = "C:\inetpub\wwwroot\Sandbox2"
          
          $rc = robocopy $source $destination /E /PURGE /R:3 /W:5 /NFL /NDL /NJH /NJS /NP
          $exitCode = $LASTEXITCODE

          if ($exitCode -ge 8) {
              Write-Error "Robocopy failed with exit code $exitCode"
              exit $exitCode
          } else {
              Write-Host "Robocopy succeeded with exit code $exitCode"
              exit 0
          }

    - task: PowerShell@2
      displayName: 'Start IIS App Pool'
      inputs:
        targetType: 'inline'
        script: |
          Import-Module WebAdministration
          Start-WebAppPool -Name "Sandbox2AppPool"

